# DeepScan AI v3.0 — Requirements
# CNN (FER-2013) + OpenCV — No FER library, No DeepFace

# ── Core ──────────────────────────────────────────────────────
streamlit>=1.32.0
numpy>=1.24.0
pandas>=2.0.0
opencv-python-headless>=4.8.0

# ── Deep Learning (CNN model) ─────────────────────────────────
tensorflow>=2.12.0

# ── Charts ───────────────────────────────────────────────────
plotly>=5.18.0

# ── Training only (not needed for Streamlit app) ─────────────
# scikit-learn>=1.3.0     # for train_test_split in train_model.py
# matplotlib>=3.7.0       # for training_curves.png

# ── Optional: install these only if running train_model.py ───
# pip install scikit-learn matplotlib
